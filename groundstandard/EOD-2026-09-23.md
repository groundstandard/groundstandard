EOD: GroundStandard: Agency - Custom Form Builder: Design, Redirects, Reporting

1. Fixed the Choice field: spaces typed into the options box were being deleted.
2. Built the Design panel: 10 presets, colours, fonts, corners, borders, inputs, button, card, effects, custom CSS. Empty means the form looks like the site.
3. Made the preview the real form: the same form.js draws it, with Light/Dark site and Thank-you/Error views.
4. Added half-width fields, so two can sit side by side.
5. Replaced the fixed adult/youth redirect with rules: if this answer, go here; else if; else. Old forms carried over unchanged.
6. Made the privacy and terms links read as links, and added a Privacy Policy | Terms of Service layout.
7. Added Send a test lead: one sample submission to the GoHighLevel webhook, same shape as a real one.
8. Added Open the live form: the real embed on a test page, so a submission can be tested end to end.
9. Pointed the reporting copy at the new n8n webhook, in the same shape the Duda widget sent, including every redirect URL.
10. Created form_submissions_v2: n8n writes the body, a trigger fills the columns and ties each lead to its form.
11. Added the Report tab: per form and per site, laid out like the Form Submissions screen, with answers, campaign, CSV and a details drawer.
12. Fixed the n8n workflow: the Supabase node was writing to columns that did not exist.
13. Fixed a live bug: the form button had been rendering in the browser's default font since launch.
14. Set the Killer B form up as a builder record with its own fonts and booking redirects, and fixed the embed for hosts that render after us.
