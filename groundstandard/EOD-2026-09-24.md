EOD: GroundStandard: Agency - Custom Form Builder: Automatic Naming, Webhook Routing by Answer

1. Made every placement name itself after the page it sits on, so a form says where it came from without anyone typing it. Works on every site we build from now on.
2. Made the embed treat "First Name", first_name and FIRST-NAME as the same field, so a CRM's existing mappings keep working.
3. Added webhook rules: if this answer, post the lead to a different CRM; everything else goes to the form's own webhook.
4. Made the rule rows one component, shared with the thank-you page rules.
5. Made Send a test lead post to every address the form can reach, each carrying the answer that routes it there.
6. Wrote the migration for the new column and a check that proves Fitness lands in one CRM and everything else in the other.
7. Confirmed the naming on a live submission: the kids page form reported "website kids cta".

Tomorrow:

1. Run the migration on the database. The access token has expired.
2. Add the Fitness rule to the Killer B form once the column is live.
